# Ralph working files (generated during runs, stored in project root)
prd.json
progress.txt
.last-branch
.dualoop-stats.json
.original-branch

# Generated by DuaLoop runs in target projects
tasks/archive/

# Archive is committed by default (contains completed PRDs for reference)
# Uncomment to ignore: archive/

# OS files
.DS_Store
